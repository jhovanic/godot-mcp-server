root = true

[*]
end_of_line = lf
insert_final_newline = true
indent_style = space
trim_trailing_whitespace = true

# Disable specifically for Markdown files (where trailing spaces create line breaks)
[*.md]
trim_trailing_whitespace = false

[Makefile]
indent_style = tab
